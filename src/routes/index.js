module.exports = {
    auth: require('./auth'),
    register: require('./register'),
    logout: require('./logout'),
    refresh: require('./refresh'),
    root: require('./root'),
    catchAll: require('./catch-all')
}